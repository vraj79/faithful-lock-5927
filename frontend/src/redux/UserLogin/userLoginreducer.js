
const initialstate={
    isAuth:false,
    token:"",
    isAuthLoading:false,
    isAuthError:false
}

const AuthReducer=(state=initialstate,{type,payload})=>{
    
    switch(type){
        
        default:{
            return state
        }
    }
}